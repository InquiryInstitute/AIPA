# AIPA — Slides (Jupyter Book)

Pilot slides for **Chapter 1**, including:

- **code-server in an iframe** (Cloud Run URL is injected at build time for GitHub Pages).
- **AI lecturer terminal demo** (WebSocket gateway + optional bridge extension in the IDE).

**Static demo page** (outer frame + canvas overlay): open **[Lecturer outer-frame demo](_static/lecturer-outer-frame-demo.html)**.

Use the left sidebar to open individual slides.
